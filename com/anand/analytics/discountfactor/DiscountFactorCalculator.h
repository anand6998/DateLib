#include <string>

using namespace std;
class DiscountFactorCalculator {
public:
    double getDisountFactor(
            double curveDate,
            double valueDate,
            double forwardValueDate,
            string ccy,
            string curveType,
            string interpolation,
            int dayCount,
            double floorRate,
            double shiftSize
        ) {
        return 0;
    }
};